# Obsidian Codex Workbench：从这里开始

这是一个本地运行的个人工作台、可安装 Skill 与空白 Obsidian Starter Vault 的组合。真实 Vault 和个人配置不属于本仓库。

## 对接收方 Codex 的第一条指令

> 请读取当前仓库的 `AGENTS.md`、`README.md`、`docs/CODEX-ONBOARDING.md` 与目标 Vault 的 `AGENTS.md`。只处理我明确指定的 Starter Vault 或 Vault 副本；先执行只读检查和配置访谈，在我确认后再创建项目、任务卡和首日演练数据。不要启用自动化、扫描 Vault 外目录或开启工作台写入。

## 最短路径

1. 运行 `scripts/bootstrap.sh --vault <新空目录>` 查看预检结果。
2. 确认后运行同一命令加 `--apply`，创建新的 Starter Vault。
3. 在工作台根目录复制 `.env.example` 为 `.env.local`，填写该 Vault 的路径，并保持写入开关为 `false`。
4. 运行 `pnpm install --frozen-lockfile && pnpm dev`，确认只读界面正常。
5. 使用临时 Vault 演练 Plan → Report → 结转；只有用户确认后才设为可写。
